    import XCTest
    @testable import SPMLogger

    final class SPMLoggerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SPMLogger().text, "Hello, World!")
        }
    }
